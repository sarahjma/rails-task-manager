<h1><%= @task.title %></h1>

<p><%= @task.details %></p>

<% if @task.completed == true %>
  <p>This task is complete</p>
<% elsif @task.completed == false %>
  <p>This task is incomplete</p>
<% end %>

<%= link_to 'Back to list', tasks_path %>

<%= link_to 'To edit', edit_task_path(@task.id)%>
